[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "nodejs", "gcc"]

[phases.install]
cmds = [
  "python3 -m pip install -r requirements.txt || python3 -m ensurepip",
  "python3 -m pip install -r requirements.txt",
  "cd server && npm install"
]
